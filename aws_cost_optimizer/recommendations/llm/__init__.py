"""
Local-LLM explanation layer for recommendations.

This package only turns an already-decided recommendation into a
human-readable explanation. It never decides eligibility, priority,
savings, or replacement resources -- those stay in the deterministic
recommendation engine.
"""
